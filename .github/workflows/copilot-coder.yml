# ============================================================================
# GitHub Copilot Coder - Caller Workflow
# ============================================================================
name: GitHub Copilot Coder

on:
  issues:
    types: [labeled]

jobs:
  copilot:
    if: contains(github.event.issue.labels.*.name, 'copilot')
    uses: copilot-org/GHES_CodingAgent/.github/workflows/copilot-coder-master.yml@main
    secrets: inherit
